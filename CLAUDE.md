# CLAUDE.md (discovery stub)

Tool-discovery stub for Claude Code. Read the canonical rules in
`config/AGENTS.md`. The Claude-specific adapter is `config/CLAUDE.md`.

Prompt-cache signature: prepend the current value from
`manual/CONTEXT.md` (`python scripts/context_signature.py`).
