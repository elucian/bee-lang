-- T0204: Complex arithmetic with labeled output
rule main:
  new a ∈ Z;
  let a := (10 + 5) * 2;
  print "Result of (10 + 5) * 2 is: ", a;
  
  let a := 10 + 5 * 2;
  print "Result of 10 + 5 * 2 is: ", a;
return;
