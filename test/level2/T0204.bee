-- @DESC: Complex arithmetic with operator precedence
-- @AI: No - Self sufficient fast test
rule main:
  new a ∈ Z;
  let a := (10 + 5) * 2;
  expect a = 30;  -- parentheses force (10+5)*2

  let a := 10 + 5 * 2;
  expect a = 20;  -- * binds tighter than + : 10 + (5*2)
return;