-- @DESC:T0206 Cycle with inline next and stop conditions (D14)
-- D14: cycle block closes with done; next is an inline jump (continue).
rule main:
  new n := 3;
  cycle do
      write n;
      let n -= 1;
      stop if n = 0;
  done;
  print;
return;

