-- @DESC:T0210 Anonymous cycle: prologue with next and stop jumps (D14)
-- D14: anonymous `cycle:` opens stable prologue without label.
rule main:
  cycle:
    new count := 0;
  do
    let count += 1;
    next if count % 2 = 0;
    stop if count ≥ 100;
    write count;
  done;
return;

