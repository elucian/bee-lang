-- @NEGATIVE: with-qualifier suppression is not implemented yet (graceful runtime error expected)
-- @DESC: With scope qualifier module is not implemented (graceful failure)
rule main:
  new x := 5;
  with math do
    let x := x * 2;
  done;
return;