-- @DESC: Match falls back to the other default branch
rule main:
  new n := 99;
  new result := 0;
  match n one:
  when 1 do
    let result := 1;
  when 2 do
    let result := 2;
  other
    let result := 999;
  done;
  expect result = 999;
return;