-- @DESC: For cycle over a stepped domain (spec/02 §3.4.3)
-- Iterates 1, 3, 5, 7, 9 over a step-2 domain (1-based).
rule main:
  new total := 0;
  for i ∈ (1..9)(2) do
    let total := total + i;
  done;
  expect total = 25;
return;