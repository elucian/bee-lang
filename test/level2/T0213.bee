-- @DESC:T0213 Then epilogue runs once after stop (D14)
-- D14: `then` block executes exactly once after loop exit.
rule main:
  cycle counter:
    new n := 0;
  while n < 10 do
    let n += 1;
    stop if n = 5;
  then
    print "stopped at n = " + n;
  done counter;
return;

