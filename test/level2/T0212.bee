-- @DESC:T0212 For cycle with inline next jump (skip evens) (D14)
-- D14: `next` is an inline transfer (continue), not a block terminator.
rule main:
  cycle:
    new total := 0;
  for i ∈ (1..10) do
    next if i % 2 = 0;
    let total := total + i;
  done;
  print total;
return;

