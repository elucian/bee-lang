-- @DESC: Ternary conditional expression selector (spec/02 §3.2)
-- `(expr_true if condition else expr_false)` — parenthesised conditional.
rule main:
  new x := 5;
  new val := (10 if x > 0 else 0);
  expect val = 10;

  new y := 0;
  new val2 := (10 if y > 0 else 0);
  expect val2 = 0;
return;