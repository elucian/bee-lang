-- @DESC: Forall-quantified for cycle (spec/02 §3.4.3)
-- Iterates the inclusive endpoint domain 1..4.
rule main:
  new total := 0;
  for ∀ i ∈ (1..4) do
    let total := total + i;
  done;
  expect total = 10;
return;