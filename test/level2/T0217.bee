-- @DESC: If-else block control flow
-- @AI: No - Self sufficient fast test
rule main:
  new x ∈ Z;
  let x := 10;
  new out := "";
  if x > 5 do
    let out := "positive";
  else
    let out := "non-positive";
  done;
  expect out = "positive";  -- 10 > 5 → then branch
return;