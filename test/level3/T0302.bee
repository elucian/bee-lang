-- @DESC: Rule anatomy with params — rule declares typed parameters and returns a named result, spec/03 §2.1
-- @AI: No - self-sufficient: assertion-only, no stdout.
rule add(a ∈ Z, b ∈ Z) => (sum ∈ Z):
  let sum := a + b;
return;

rule main:
  new r := add(3, 4);
  expect r = 7;
return;
