-- @DESC: Multi-result deconstruction — new s, d := rule_call(), spec/03 §2.3
rule divmod(a ∈ Z, b ∈ Z) => (q, r ∈ Z):
  let q := a / b;
  let r := a % b;
return;

rule main:
  new q, r := divmod(17, 5);
  print q;
  print r;
return;
