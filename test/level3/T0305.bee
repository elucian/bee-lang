-- @FROZEN: Generated from /spec/. Immutable ground truth for AI agents.
-- @DESC: Expect postcondition pass — expect condition holds, no error raised, spec/03 §4
rule main:
  new a ∈ Z; new b ∈ Z;
  let a := 10;
  let b := 20;
  expect a + b = 30;
  print "expect passed";
return;
