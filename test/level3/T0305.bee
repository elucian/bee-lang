-- @DESC: Expect postcondition pass — expect holds and no error is raised, spec/03 §4
-- @AI: No - self-sufficient: assertion-only, no stdout.
rule main:
  new a ∈ Z; new b ∈ Z;
  let a := 10;
  let b := 20;
  expect a + b = 30;
  expect b - a = 10;
return;
