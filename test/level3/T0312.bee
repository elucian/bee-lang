-- @FROZEN: Generated from /spec/. Immutable ground truth for AI agents.
-- @DESC: Nested rule calls — one rule calls another rule and captures result
rule double(n ∈ Z) => (r ∈ Z):
  let r := n * 2;
return;

rule main:
  new x := double(21);
  print x;
return;
