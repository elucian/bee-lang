-- @DESC: Assert precondition pass — assert condition holds, no warning emitted, spec/03 §4
rule main:
  new a ∈ Z; new b ∈ Z;
  let a := 10;
  let b := 20;
  assert a < b;
  print "assert passed";
return;
