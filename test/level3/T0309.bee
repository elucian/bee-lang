-- @DESC: Scoping variable locality — rule-local variables are not visible outside, spec/03 §4
rule helper:
  new local_var ∈ Z;
  let local_var := 42;
  print local_var;
return;

rule main:
  print "locality ok";
return;
