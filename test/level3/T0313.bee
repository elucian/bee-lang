-- @DESC: Recursion — factorial via recursive rule call
rule factorial(n ∈ N) => (r ∈ N):
  if n ≤ 1 do
    let r := 1;
    exit;
  done;
  let r := n * factorial(n - 1);
return;

rule main:
  new f := factorial(5);
  print f;
return;
