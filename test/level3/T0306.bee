-- @FROZEN: Generated from /spec/. Immutable ground truth for AI agents.
-- @NEGATIVE
-- @DESC: Expect postcondition fail — expect raises E0303 and halts, spec/03 §4
rule main:
  new a ∈ Z; new b ∈ Z;
  let a := 10;
  let b := 20;
  expect a + b = 99;
  print "should not reach here";
return;
