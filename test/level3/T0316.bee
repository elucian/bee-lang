-- @DESC: Lambda inline — an anonymous lambda is called immediately with arguments, spec/07 §6.3
-- @AI: No - self-sufficient: assertion-only, no stdout.
rule main:
  new result := λ((x, y) => x + y)(10, 20);
  expect result = 30;
return;
