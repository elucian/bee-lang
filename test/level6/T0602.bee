-- @ENABLED: Reset by user
-- @DESC: Anonymous try block no scope
-- miss catches the rest demo
rule main:
  try
        raise {999, "unknown error"};
  case $error.code = 1 do
        print "wrong case";
  error
        print "missed " + $error.code;
  done;
return;
