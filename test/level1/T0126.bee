-- @DESC:file: type_inference.bee
-- Type inference demo
rule main:
  new a := 10;
  new r := 0.5;
  new s := "text";
  new f := True;
  expect a.type() is Z;
  expect r.type() is R;
  expect s.type() is U;
  expect f.type() is B;
return;
