-- @DESC:file: type_inference.bee
-- Type inference demo
rule main:
  new a := 10;
  new r := 0.5;
  new s := "text";
  new f := True;
  expect a.type() == Z;
  expect r.type() == R;
  expect s.type() == U;
  expect f.type() == B;
return;
