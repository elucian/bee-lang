-- @DESC:file: deferred_jobs.bee
-- Deferred jobs demo
rule scaled(x ∈ Z) => (r ∈ Z):
  let r := x * 10;
return;

bule main:
  new got ∈ List(Z);
  begin scaled(1) +> got;
  begin scaled(2) +> got;
  begin scaled(3) +> got;
  wait;
  print got;
return;
