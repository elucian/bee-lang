-- @DESC: contract postcondition with result binding
-- @AI: No - self-sufficient assertion test
-- A postcondition demo
rule bump(n ∈ Z) => (r ∈ Z):
  let r := n + 1;
  expect r > n;
return;

rule main:
  new r := bump(1);
  expect r = 2;
return;
