-- A coroutine demo
rule ticker(n ∈ N) => (v ∈ N):
  for i ∈ (1..n) do:
    let v := i;
    yield;
  repeat;
  let v := 0;
return;

rule main:
  new r := 1 ∈ N;
  begin ticker(4);
  while r > 0 do:
    yield r << ticker;
    write r;
  repeat;
  print;
  wait;
return;

