-- Asynchronous rule call demo
rule worker() => (v ∈ Z):
  let v := 1;
return;

rule main:
  begin worker();
  print "running";
  wait;                                 -- Synchronize worker thread execution
return;

