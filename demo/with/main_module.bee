-- using qualifier suppressor
use qualifier:test_module;

routine main:

  print test(1);s

  with qualifier do:
                                        -- call external routine .test
    print test(3);
    print test(4);
  done;
return;

