# AGENTS.md (discovery stub)

This is a tool-discovery stub. The canonical agent-rule contract for this
repository lives in `config/AGENTS.md`.

> Read `config/AGENTS.md`. It is the single source of truth for AI-agent
> behavior here. This stub adds nothing and must never fork rules.

Prompt-cache signature: prepend the current value from
`manual/CONTEXT.md` (`python scripts/context_signature.py`) before any task
prompt.
